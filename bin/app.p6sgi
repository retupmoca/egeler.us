use Crust::Builder;
use Crust::Middleware::StackTrace;

use Site;
use Config;

Config.load("/etc/perlsite.conf");
my $site = Site.new;

my $app = sub (%env) { $site.handle(%env) }

builder {
    enable 'StackTrace';
    $app;
};
